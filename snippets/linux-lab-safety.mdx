import LinuxCommandGuidance from './linux-command-guidance.mdx';

<LinuxCommandGuidance />

<Warning>
  Run commands that change users, storage, services, permissions, or networking only in a disposable lab environment. Confirm device names, usernames, and paths before using `sudo` or destructive commands.
</Warning>

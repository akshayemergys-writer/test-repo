import LinuxCommandGuidance from './linux-command-guidance.mdx';

<LinuxCommandGuidance />

<Note>
  Use `apt` and `dpkg` on Debian-based systems. Use `dnf` and `rpm` on Fedora, RHEL, and other RPM-based systems. Choose the commands that match your distribution.
</Note>
